{
  "name": "hono-better-auth-ex1",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d"
  },
  "dependencies": {
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
